import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Catalog | Web-app",
};

export default function Catalog() {
  return (
    <div>
      <h1>All products</h1>
    </div>
  );
}
